Place SSL certificates in this directory for nginx HTTPS server block:
- fullchain.pem
- privkey.pem

For local development without certificates, keep nginx on HTTP only or generate self-signed certs.
